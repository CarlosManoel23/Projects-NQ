window.addEventListener('load', () => {
    const loader = document.getElementById('loader');
    
    // Adiciona a classe que faz o loader sumir suavemente
    loader.classList.add('loader-hidden');
});